DropdownListItem
  Hitbox `${on_click}`

Dropdown
  Hitbox `${on_click}`
  Text `${active}` 2,2
  Icon drop -8,0,8,-0
  List `${drops}`
    DropdownListItem

List
  ScrollableView
    `${items: Item0 Item1}`
    `${content}` `${.Item0}` 0,0,-0
    `${content}` `${.Item1}` =,v,-0

ScrollableView
  Background
  HorizontalBar -6,0,4,-2
    Thumb 2,2
  ClipView `${content}` 2,2,-8,-2

